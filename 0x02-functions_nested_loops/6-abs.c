#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 *function that computes the absolute value of an integer
 * Return: Always 0.
 */

int _abs(int n)
{

if (n >= 0)
{
return (n);
}
else
{
return (n * -1);
}

_putchar('\n');



}
