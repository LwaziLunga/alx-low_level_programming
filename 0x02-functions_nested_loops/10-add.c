#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 * a function that adds two integers and returns the result
 * Return: Always 0.
 */

int add(int num1, int num2)
{
return (num1 + num2);
_putchar('\n');
	
}
